@inherits LayoutComponentBase
@inject ToastService toastService

<div class="page">
    <div class="sidebar @(sidebarExpanded ? "sidebar-expanded" : "")">
        <NavMenu OnToggle="OnSidebarToggle" />
    </div>

    <main>
        <article class="content px-3">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

<Toasts Placement="ToastsPlacement.BottomRight" AutoHide="true" Delay="4000" />

@code {
    private bool sidebarExpanded = false;

    private void OnSidebarToggle(bool isExpanded)
    {
        sidebarExpanded = isExpanded;
    }
}
